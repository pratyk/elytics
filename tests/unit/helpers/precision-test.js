import { precision } from 'elytics/helpers/precision';
import { module, test } from 'qunit';

module('Unit | Helper | precision');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = precision([42]);
  assert.ok(result);
});
